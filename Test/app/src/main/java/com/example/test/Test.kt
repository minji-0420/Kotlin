package com.example.test


//lines.map { List(abs(it[1] - it[0])) { r -> List(2) { c -> (it[0] + r) + c } } }

class Solution {
    fun solution(arr1: Array<IntArray>, arr2: Array<IntArray>): Array<IntArray> {
        var answer = arrayOf<IntArray>()
        return answer
    }
}